{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "a11y-agent": {
      "type": "stdio",
      "command": "node",
      "args": ["${PLUGIN_ROOT}/packages/mcp/bundle/index.cjs"],
      "cwd": "${PLUGIN_ROOT}/"
    }
  }
}
