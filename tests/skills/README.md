# Skill script tests

The twelve skills contain guidance and focused references but no executable
scripts. Repository contracts require any future `skills/<name>/scripts/`
directory to add tests under this directory before CI can pass.
